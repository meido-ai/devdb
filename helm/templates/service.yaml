apiVersion: v1
kind: Service
metadata:
  name: {{ include "devdb.fullname" . }}
  labels:
    {{- include "devdb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devdb.selectorLabels" . | nindent 4 }}